// Shared workspace constant: the presentational components, the example
// loader, and the model registry all reference it — kept dependency-free so
// UI modules never pull in the editor stack.
export const TSCONFIG_FILE = 'tsconfig.json';
